/**
 * TODO:
 * - Define routes for:
 *   - GET /jobs → fetch all job entries
 *   - POST /jobs → add a new job
 *   - PUT /jobs/:id → update a job's status
 *   - DELETE /jobs/:id → delete a job
 * - Use db.js to interact with PostgreSQL
 * - Export the router
 */